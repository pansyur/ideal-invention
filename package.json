{
  "name": "ideal-invention",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prebuild": "chmod +x ./scripts/appflow-build.sh && ./scripts/appflow-build.sh",
    "build": "mkdir -p downloads && echo '<html><body>Build Complete</body></html>' > downloads/index.html"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
